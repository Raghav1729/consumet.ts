// import { MediaFormat, StreamingServers } from '../../src/models';
// import { MOVIES } from '../../src/providers';

// jest.setTimeout(120000);

// const vidSrc = new MOVIES.VidSrc();

// test('returns a filled object of streaming servers', async () => {
//   const data = await vidSrc.fetchEpisodeServers('66788', MediaFormat.TV, 1, 1);
//   console.log(data);
//   expect(data).not.toEqual([]);
// });

// // test('returns a filled object of streaming sources', async () => {
// //   const data = await vidSrc.fetchEpisodeSources('66788', MediaFormat.TV, 1, 1, StreamingServers.VidCloud);
// //   expect(data).not.toEqual([]);
// // });
